# Copier ce fichier en `.env.local` (jamais commité, voir .gitignore) pour tester
# la fonction /api/contact en local avec `vercel dev`.
# Clé récupérée sur https://resend.com/api-keys (permission "sending_access" suffit).
RESEND_API_KEY=re_xxxxxxxxxxxxxxxxxxxxxxxxxxxx
